<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!-- Used only when signing with a real Developer ID (CI). Notarization
     requires the hardened runtime, and a frozen Python needs these two
     relaxations: ctypes/libffi allocates executable memory, and the
     bundled dylibs are signed by us rather than by Apple's toolchain. -->
<plist version="1.0">
<dict>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
</dict>
</plist>
